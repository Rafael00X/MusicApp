<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <group
        android:id="@+id/media"
        android:checkableBehavior="single">
        <item android:title="Songs"
            android:icon="@drawable/ic_song"
            android:id="@+id/songs"/>
        <item android:title="Albums"
            android:icon="@drawable/ic_album"
            android:id="@+id/albums"/>
        <item android:title="Artists"
            android:icon="@drawable/ic_artist"
            android:id="@+id/artists"/>
        <item android:title="Playlists"
            android:icon="@drawable/ic_playlist"
            android:id="@+id/playlists"/>
    </group>
    <group
        android:id="@+id/others"
        android:checkableBehavior="single">
        <item android:title="Manage Media"
            android:id="@+id/manage_media"/>
    </group>
</menu>